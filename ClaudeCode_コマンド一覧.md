# Claude Code コマンド一覧（自分用リファレンス）

作成日: 2026-08-15
このファイルは後から見返すためのメモです。最新情報は公式ドキュメント
（https://code.claude.com/docs/）を参照してください。

---

## 1. スラッシュコマンド（会話中に `/コマンド名` で実行）

### セッション管理
| コマンド | 説明 |
|---|---|
| `/help` | 使えるコマンドの一覧を表示 |
| `/clear [名前]` | 会話をリセット（プロジェクトの記憶=CLAUDE.mdなどは保持） |
| `/resume` | 過去の会話を再開する |
| `/branch [名前]` | 今の会話から分岐して別の方向を試す |
| `/background [指示]` | 今のセッションをバックグラウンドタスクにする |

### モデル・パフォーマンス
| コマンド | 説明 |
|---|---|
| `/model [モデル名]` | 使用モデルを切り替え（sonnet / opus / haiku / fable） |
| `/effort [レベル]` | 推論の強さを調整（low / medium / high / xhigh / max） |
| `/fast` | 高速・低コストモードの切り替え（Opus系で利用可） |
| `/compact` | 会話を要約して文脈（コンテキスト）を空ける |

### コード品質・レビュー
| コマンド | 説明 |
|---|---|
| `/code-review [レベル]` | 差分（diff）をレビューし、バグや改善点を検出 |
| `/security-review` | セキュリティ上の脆弱性をチェック |
| `/diff` | 差分をインタラクティブに表示 |
| `/batch <指示>` | 大規模な変更を並列で実行 |

### 設定・状態確認
| コマンド | 説明 |
|---|---|
| `/config [key=value]` | 各種設定の調整 |
| `/permissions` | ツールのアクセス権限を管理 |
| `/memory` | CLAUDE.md などの記憶ファイルを編集 |
| `/context [all]` | コンテキスト使用量を可視化 |
| `/usage` | API使用量・費用の表示 |
| `/init` | プロジェクト用の CLAUDE.md を初期生成 |

> 上記以外にも `/agents`（サブエージェント管理）、`/mcp`（MCP連携）、
> `/bug`（不具合報告）など状況に応じたコマンドが表示されることがあります。
> 迷ったら `/help` で最新の一覧を確認するのが確実です。

---

## 2. 自分でスラッシュコマンドを作る（カスタムコマンド）

`.claude/commands/` フォルダに Markdown ファイルを置くと、独自の
スラッシュコマンドを作れます。

```markdown
---
name: my-command
description: このコマンドの説明
---
（実行させたい処理・指示をここに書く）
```

- ファイル名がそのままコマンド名になる
- プロジェクト単位（`.claude/commands/`）でもユーザー単位
  （`~/.claude/commands/`）でも定義可能

---

## 3. ターミナルから起動するときのコマンド（CLI）

### 基本の起動方法
| コマンド | 説明 |
|---|---|
| `claude` | 対話セッションを開始 |
| `claude "質問文"` | 最初のプロンプト付きで起動 |
| `claude -p "質問文"` | 非対話モード（1回応答して終了。スクリプト向け） |
| `claude -c` | 直前の会話を継続 |
| `claude --model <名前>` | 使用モデルを指定して起動 |

### セッション管理系オプション
| オプション | 説明 |
|---|---|
| `--resume` | 特定のセッションを再開 |
| `--background` | バックグラウンドエージェントとして起動 |
| `--max-turns <n>` | 最大やり取り回数を制限 |
| `--max-budget-usd <金額>` | 使用予算の上限を設定 |

### 権限・セキュリティ系オプション
| オプション | 説明 |
|---|---|
| `--permission-mode <mode>` | 権限モード指定（default / plan / auto / dontAsk / bypassPermissions） |
| `--add-dir <パス>` | 追加でアクセスを許可するディレクトリ指定 |

### その他
| オプション | 説明 |
|---|---|
| `--effort <レベル>` | 推論レベル指定（low〜max） |
| `--system-prompt <文>` | システムプロンプトを上書き |
| `--settings <パス\|JSON>` | 設定の上書き |
| `--debug` | デバッグログを有効化 |

---

## 4. キーボードショートカット

| 操作 | キー | 内容 |
|---|---|---|
| 送信 | `Enter` | メッセージを送信 |
| キャンセル | `Ctrl+C` | 実行中の操作を中断 |
| 終了 | `Ctrl+D` | Claude Code を終了（2回押しで確定） |
| 履歴検索 | `Ctrl+R` | コマンド履歴を全文検索 |
| 権限モード切替 | `Shift+Tab` | 権限モードを順に切り替え |
| 画面クリア | `Ctrl+L` | 表示だけリセット（入力中の文章は保持） |
| 改行 | `Ctrl+J` | 送信せずに改行を挿入 |
| 外部エディタ | `Ctrl+G` | 外部エディタで入力内容を編集 |
| 元に戻す | `Ctrl+_` | 直前の操作を取り消し |
| TODO表示切替 | `Ctrl+T` | チェックリスト表示の切り替え |

ショートカットは `~/.claude/keybindings.json` で自分好みにカスタマイズできます。

---

## 5. CLAUDE.md ファイルについて

プロジェクトの「永続的な指示書」で、セッション開始時に自動で読み込まれます。
このファイルは種類ごとに置き場所が変わります。

| 種類 | 置き場所 | 用途 |
|---|---|---|
| 管理ポリシー | 組織側で管理 | 組織全体のルール |
| ユーザー指示 | `~/.claude/CLAUDE.md` | 自分の全プロジェクト共通の好み |
| プロジェクト指示 | `./CLAUDE.md` または `./.claude/CLAUDE.md` | チームで共有するルール |
| ローカル指示 | `./CLAUDE.local.md` | 自分専用（Git管理対象外にするのが推奨） |

ビルドコマンド・コーディング規約・アーキテクチャ・命名規則などを書いておくと、
毎回の説明が省ける。長くなりすぎないよう200行程度を目安に簡潔にまとめるのがコツ。
特定ファイルだけに適用したい条件付きルールは `.claude/rules/` に置ける。

---

## 6. サブエージェント（Agent）機能

特定の作業だけを任せる「専用の担当」を呼び出せる機能。詳細な調査や
大量の出力処理を、メインの会話から切り離して行える。

定義場所: `~/.claude/agents/`（自分専用）または `.claude/agents/`（プロジェクト用）

```yaml
---
name: agent-name
description: このエージェントの役割説明
tools: Read, Grep, Glob
model: sonnet
---
（このエージェントのシステムプロンプト本体）
```

主な設定項目:
- `tools` … 使わせるツールを制限
- `model` … 使用モデルを指定
- `permissionMode` … 権限モード
- `memory` … 記憶機能の有効化（user / project / local）

呼び出し方は「自然言語でお願いする」「`@エージェント名` でメンションする」
「CLI で `--agent` フラグを使う」のいずれか。

---

*このファイルは Claude Code に調査させて作成したメモです。コマンド体系は
アップデートで変わることがあるため、正確な最新情報が必要な場合は
公式ドキュメント（https://code.claude.com/docs/）を確認してください。*
